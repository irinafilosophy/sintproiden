import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Hello, World!")
            .frame(width: 175, height: 100)
            .background(Color.blue) // Just to visualize the frame
    }
}
